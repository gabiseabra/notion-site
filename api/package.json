{
  "name": "@notion-site/api",
  "version": "0.1.0",
  "private": true,
  "main": "dist/server.js",
  "types": "dist/server.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "start": "node dist/server.js",
    "dev": "nodemon --watch 'src/**' --watch '../common/src/**' --ext ts,tsx,js,json --exec 'npm run build && dotenvx run -f ../.env -- npm run start'"
  },
  "dependencies": {
    "@notion-site/common": "*",
    "@notionhq/client": "^2.2.1",
    "@orpc/server": "^1.13.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "json5": "^2.2.3",
    "morgan": "^1.10.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/morgan": "^1.9.10",
    "nodemon": "^3.1.11"
  }
}
